# Ramdom number generation static functions

class Random:

    # Ideally return an array of random numbers as will be
    # used in Monte-Carlo simulation
    def get_box_muller_random_number():
        
        # return array for input collection size
        return 0
        